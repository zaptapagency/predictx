# Railway auto-deploys from GitHub webhook when it detects new commits
# This workflow is disabled - manual deployment is not needed
name: Deploy to Railway (Auto)

on:
  workflow_dispatch:  # Manual trigger only for reference

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ Railway auto-deploys from webhook. No manual action needed."
